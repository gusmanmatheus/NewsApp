<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imgHeadlineIv"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_margin="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/titleHeadlineTv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        android:text="24sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.2"
        app:layout_constraintBottom_toBottomOf="@id/imgHeadlineIv"
        android:textColor="@color/black"
        app:layout_constraintStart_toEndOf="@id/imgHeadlineIv"
        app:layout_constraintTop_toTopOf="@id/imgHeadlineIv" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/imgHeadlineIv"
        app:layout_constraintEnd_toEndOf="@+id/imgHeadlineIv"
        app:layout_constraintStart_toStartOf="@+id/imgHeadlineIv"
        app:layout_constraintTop_toTopOf="@+id/imgHeadlineIv" />

</androidx.constraintlayout.widget.ConstraintLayout>